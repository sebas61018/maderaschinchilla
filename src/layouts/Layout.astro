---
import Header from "../components/header.astro";
import Fondoimagen from "../components/fondoimagen.astro";
import Contenido from "../components/contenido.astro";
import Galeria from "../components/galeria.astro";
import Fondomovido from "../components/fondomovido.astro";
import GaleriaVi from "../components/galeriaVi.astro";
import Fouter from "../components/fouter.astro";
import '../styles/global.css';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/assets/logo/logo.ico" />
	<link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap"
    rel="stylesheet"/>
    <meta name="generator" content={Astro.generator} />
    <title>Madera Chinchilla</title>
  </head>

  <body class="font-poppins bg-gray-50 scroll-smooth">

    <section id="inicio">
      <Header />
    </section>

    <section  class="relative overflow-hidden h-[400px] md:h-[500px] lg:h-[600px]">
	    <Fondoimagen />
    </section>

    <section class="max-w-5xl mx-auto px-6 py-16 md:py-20 lg:py-24 text-center scroll-reveal">
      <p class="text-base md:text-lg lg:text-xl text-gray-800 leading-relaxed font-medium transition-all duration-700 opacity-0 translate-y-8 reveal-descripcion">
        Nuestras obras son sinónimo de <span class="text-[#A67C52] font-semibold">calidad, estilo y funcionalidad</span>. <br />
        Ofrecemos atención personalizada y profesional para crear espacios únicos y prácticos en tu hogar.
        <br /><br />
        Especialistas en el diseño y fabricación de
        <span class="font-semibold text-gray-900">
          cocinas integrales, closets, vestieres, puertas interiores, muebles para baños, escritorios, centros de televisión y repisas
        </span>,
        utilizamos siempre los mejores materiales y acabados para garantizar resultados excepcionales.
      </p>
    </section>
    
    <section id="portafolio" >
      <Contenido 
      title = "NN"
      description = "NN"
        imageUrl="/assets/contenido/contenido1.jpg"
      />

      <Contenido 
        title = "NN"
        description = "NN"
        imageUrl="/assets/contenido/contenido2.jpg"
        reverse={true}
      />

      <Contenido 
        title = "NN"
        description = "NN"
        imageUrl="/assets/contenido/contenido3.jpg"
      />
    </section>

    <section id="servicio" >
      <Galeria/>
    </section>

    <section >
      <Fondomovido/>
    </section>

    <section class="max-w-5xl mx-auto px-6 py-12 md:py-16 lg:py-20 text-center">
      <p class="text-base md:text-lg lg:text-xl text-gray-700 leading-relaxed">
        En Madera Chinchilla combinamos calidad, estilo y funcionalidad para crear espacios únicos. 
        Diseñamos y fabricamos cocinas, closets, muebles y más, 
        con materiales de primera y acabados impecables.
      </p>
    </section>

    <section id="galeria" >
      <GaleriaVi/>
    </section>

    <section class="max-w-5xl mx-auto px-6 py-16 md:py-20 lg:py-24 text-center scroll-reveal">
      <p class="text-lg md:text-xl lg:text-2xl text-gray-800 leading-relaxed font-medium transition-all duration-700 opacity-0 translate-y-8 reveal-text">
        <span class="text-[#A67C52] font-semibold">Transforma</span> tus espacios con diseño, calidad y estilo único. 
        <br class="hidden md:block" />
        <span class="text-[#A67C52] font-semibold">¡Haz realidad</span> el hogar de tus sueños con <span class="font-bold">Madera Chinchilla!</span>
      </p>
    </section>

    <section id="contacto">
      <Fouter/>
    </section>


  </body>

</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
	font-family: 'Poppins', sans-serif;
  }
</style>

<script is:inline>
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener("click", function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute("href"));
      if (target) {
        window.scrollTo({
          top: target.offsetTop - 80, // Ajusta según la altura de tu header
          behavior: "smooth"
        });
      }
    });
  });


  document.addEventListener("DOMContentLoaded", () => {
    const target = document.querySelector('.reveal-text');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add("opacity-100", "translate-y-0");
          entry.target.classList.remove("opacity-0", "translate-y-8");
        }
      });
    }, { threshold: 0.3 });

    observer.observe(target);
  });
  
  document.addEventListener("DOMContentLoaded", () => {
    const target = document.querySelector('.reveal-descripcion');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add("opacity-100", "translate-y-0");
        }
      });
    }, { threshold: 0.3 });

    observer.observe(target);
  });

</script>